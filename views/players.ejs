<section class="page-form-ele page">

    <section class="panel panel-default">
        <div class="panel-heading"><strong><span class="glyphicon glyphicon-th"></span> <span i18n="search-a-player"></span></strong></div>
        <div class="panel-body">
            <div class="row">
                <div class="col-sm-6">
                    <input type="text"
                           ng-model="selected"
                           typeahead="playerName for playerName in playerNames | filter:$viewValue | limitTo:8"
                           class="form-control"
                           i18n="i18n-placeholder"
                           placeholder="search">
                </div>
            </div>
        </div>
    </section>

    <table class="table">
        <thead>
            <tr>
                <th i18n="pseudo"></th>
                <th i18n="status"></th>
                <th i18n="ratio"></th>
                <th i18n="score"></th>
            </tr>
        </thead>
        <tbody infinite-scroll-distance="3">
            <tr class="animate" ng-repeat="player in players | filter:{ pseudo: selected }">
                <td><a href="/#/profile/{{player.id}}" alt="{{player.pseudo}}">{{player.pseudo}}</a></td>
                <td ng-if="player.online"><span class="label label-success" i18n="online"></span></td>
                <td ng-if="!player.online"><span class="label label-danger" i18n="offline"></span></td>
                <td><em>{{player.avgRatio}}<em></td>
                <td><em>N/A (yet)<em></td>
            </tr>
    </tbody>
    </table>

</section>
